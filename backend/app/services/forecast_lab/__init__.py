"""Forecast Lab: isolated prediction pipeline (no legacy ml/ml2 imports).

Import concrete modules directly (e.g. ``inference.build_summary``) to avoid
eagerly loading optional heavy dependencies when only utilities are needed.
"""
